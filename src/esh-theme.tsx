import { createTheme } from "@mui/material";

const eshTheme = createTheme({
  typography: { fontFamily: "Alef" },
  palette: { primary: { main: "#6A6D82" }, secondary: { main: "#ffffff" } },
});

export default eshTheme;